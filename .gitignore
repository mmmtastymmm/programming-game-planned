/target

# Linux crash core dumps (kernel core_pattern core.%e.%p.%h.%t). Deliberately
# narrower than a bare `core`/`core.*`, which would silently swallow a future
# core.rs module or core/ directory.
core.*.[0-9]*
