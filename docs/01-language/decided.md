*Part of [01-language](../01-language.md).*

# Decided

- **Cost table is moddable** — base `costs.ron` + per-map/biome overlays, so maps can stress specific program designs (see [Cycle Costs](cycle-costs.md)).
- **Recursion allowed, small stack cap** — base 4 frames, overflow is a standard fault (see Tier 4).
- **Reserved handler templates** (redesign 2026-07-13, supersedes the 2026-07-12 unified handler): every signal — `error`, `hurt`, `bump`, `bumped`, `boot`, with `abort` and `recall` **fully engine-reserved** (zero-size windows) — owns a template of **forced prologue → editable window → forced epilogue**. Prologues carry the unskippable `handler_init()` flinch (~15 ticks; boot's is the forced `upload_log()`); epilogues carry the hard-coded exits (abort is *all* epilogue: `upload_log()` + `become_disabled()`); windows are per-signal `on <signal>:` blocks taking **any code** (the instruction caps and `signal_safe` restriction were deleted 2026-08-02 — see below). Windows ship with **factory contents** — the engine default as real, replaceable, inspectable Pyrite (error's is `upload_crash_dump()`, the guaranteed-debuggability floor). Constants live in the cost table.
- **Every handler state is broadcast visually** — each signal has a globally fixed color and icon shown in the bot's thought cloud on entry (distinct from program colors); handler states are readable at a glance by anyone with vision, friend or foe (pillar 2).
- **Collision signals**: rammer gets `bump`, victim gets `bumped`; the factory windows implement the asymmetric stun (rammer's `wait(35)` + init = 50 ticks; victim's flinch-only = 15) — your window replaces the stun, never the chassis damage. Engine-driven walks (recall) never raise `bump` on the mover.
- **Double-handle = abort** (2026-07-13, softening the original explosion rule): any event that would start a handler while another template runs — any combination, anywhere in the sandwich including forced epilogues, factory contents included — forces the bot into `abort`. Abort itself is un-interruptible: no player code to fault, and signals during it are absorbed. Instant explosions are gone; the counterweight to hurt's unbounded handler time is losing the bot to the wreck race early. (Answers Q50 — no humble carve-out needed.)
- **Abort is fully engine-reserved** (like recall): no `on abort:` window exists. Every death runs the forced `upload_log()` then `become_disabled()` — the logs always reach the cloud, and the wreck's self-destruct countdown starts: field-repair in time rescues it (XP intact); expiry is the **only** way a bot explodes. There are no last words — the black box is whatever the bot logged while alive. Wrecks can also be `salvage()`d (by anyone) for a cut of their build materials.
- **Every destruction drops a Black Box** (local logs + cause + env snapshot, Q58); information always survives — XP is the only thing gambled. Rescued/printed bots run a **Boot Sequence**: forced `upload_log()` if the buffer is non-empty, then execute from line 1. Forced engine behaviors are ordinary registry builtins (`upload_crash_dump`, `upload_log`; `become_disabled` is engine-only — the player scuttle verb is `abort()`, Q76) — one code path.
- **Boot participates in double-handle** — any signal mid-boot aborts the bot back into a wreck, so rescues must be timed to safety.
- **Colored program slots** — every bot carries one of the colony's colored programs, visibly tinted. One color = one Printer; printer count is gated by **controlled nests, quadratically** (3rd color: 1 nest; then 3, 6, 10 …) — named palette through 9 colors, **uncapped** beyond. Secrecy is per-color attrition: each enemy salvage grants +N% (default 5%) **permanent** decryption of that color, surviving redeploys ([08-multiplayer.md](../08-multiplayer.md)). A *new* slot starts at 0%.
- **The remainder printer is indestructible** (2026-07-17, answers Q88). The remainder bucket is the faction's lowest-entity-id printer (the base color slot, un-editable rules), and it can **never** enter Ruined or Dormant — so its wearers are never stranded as un-recallable, un-scrappable ghosts drawing upkeep with no way to re-home. It's one of the two free base slots, never nest-bound, so the dormancy path (Q87, [04-enemies.md](../04-enemies.md)) never targets it anyway; this makes that a guarantee rather than an accident. The opening's shipped ruined **Red** printer is a *non-remainder* color — still fully breakable and a repair objective.
- **Recall** — the engine-owned signal, engine-fixed and un-writable: the target-share allocation re-colors claimed bots at their new printer (XP kept); an over-capacity colony recalls its lowest-XP fleet bot (ghosts exempt) for scrap — politely, re-selecting if its target is mid-template (Q85). Recall is an interrupt context — double-handle applies all the way home.
- **Logging is ordinary functions** — `log`, `upload_log`, `upload_crash_dump` are costed builtins, so telemetry and black boxes are player-built, not engine magic (with the forced crash dump as the guaranteed floor).
- **Loops** — `while` + `break`/`continue` at Tier 3; Python-style `for x in container` with containers at Tier 5. Implicit program loop stays; `while True:` legal but redundant.
- **Generic fallible queries** — `exists(kind)` / `closest(kind)` with bare kind constants replace the old `nearest_*` / `*_exists` builtin family. `closest` returns the builtin `Result` enum; unwrap with `.expect()` (faults on `Err`) or handle fault-free with `match`.
- **Gating is per-function, never per-argument** (Q80) — kind constants are shadowable names (`x = enemy; closest(x)` would defeat any per-kind gate), so `closest` is start-kit for every kind.
- **Stale handles fault** (Q80) — acting on an entity you no longer perceive (and don't know via map knowledge) is a failed action like any other miss.
- **Fault identities are pre-bound constants** (`err_timeout`, `err_tool_jam`, `err_unknown_contact`, `err_payload`, …) — a data registry, `==`-comparable, returned by `last_error()`.
- **Enum identity is structural** (Q80) — name + variant + arity match across artifacts: spoofing a decrypted protocol is possible by construction; the defense is secrecy and rotation, not types.
- **Yields are typed** — `mine()` produces the node's specific resource; cargo is a typed manifest, never generic "ore units", so hauling programs route by kind (`cargo_count(kind)`, `withdraw(kind)` — Q79) and refinery buffers accept only their recipe's inputs. Every resource, raw or refined, has its own kind constant (Q69/round 4).
- **Creeping is a movement argument** (2026-07-26, answers Q103) — the pathing builtins take a trailing **`creep=False`** flag beside the Q95 paint args: `creep=True` makes each step much slower (`creep_step_pct`, tuning) and cuts the bot's signature (`creep_signature`), so it is heard only from much closer. No `creep()` verb: stealth belongs to whatever movement you were already doing, which is why `explore(creep=True)` — a scout that surveys without announcing itself — falls out for free. Sight is unaffected (Q74) and heard-at floors at 1, so creeping past a seeing circle is still impossible and adjacency always detects. Free with its verbs, like every other call argument (Q96).
- **Paint routing is per-call** (2026-07-26, answers Q95) — the pathing builtins (`move_to`, `wander`, `explore`) take trailing optional **`only=` / `avoid=`** paint arguments: a pre-bound paint-color constant (like the kind and level constants) or a Tier-5 list of them — a bare color is legal at Tier 0, so `move_to(depot, only=green)` needs no collections. The constraint scopes to that one call: route policy can differ line by line, fault restarts have nothing to clear, and the sim gains no state beyond the paint map itself. Omitted args = paint-blind — paint is invisible to code that hasn't opted in, so every existing program is untouched. The rejected homes: a per-color deploy rule (policy belongs at the call site, next to the movement it shapes) and env keys (Q59 keeps them engine-defined ints). Paint stays a general-purpose signal layer — the `paint_at()` sensor hook stands, and more color uses may come. Paint ownership is Q97 ([05-terrain.md](../05-terrain.md) Tile Composition).
- **The router's contract** (2026-07-26, answers Q96) — three rulings. **`unpainted` is a color**: a pre-bound constant like the palette's, so the args stay literal — `only=green` is a strict road, `only=[green, unpainted]` admits bare ground, `avoid=unpainted` pins a bot to painted surface; no neutral-ground special case, and the paint eraser is just painting `unpainted`. **Forbidden paint is impassable-like-water** for that one route search: the args edit the passability mask and nothing else, so an unreachable destination fires the standard no-path fault — no final-step exemption, no silent nearest-legal stop. **The args are free with their verbs** — no per-argument gating machinery. The strictness + Tier-5-lists pairing makes `avoid=` the Tier-0 idiom (one color to shun) and strict `only=` roads ergonomic from Tier 5 — blacklist before whitelist, a natural ladder.
- **The verb layer is complete** (2026-07-14, answers Q79): `study()` (start-kit — the Cache-learning verb), `withdraw(kind)` (the take verb; `deposit()` generalizes to any accepting structure), `cargo_count(kind)`, `wander()` + `explore()` (dumb and smart exploration — explore picks a random fogged tile and surveys it), `health_low()`, `repair()` (field repair on wrecks), `guard(entity)`/`escort(entity)` (entity-anchored, never tiles), `hijack(wreck)`, `scan_resources()`, `my_quirks()`/`has_quirk()` (quirk names are pre-bound constants, not a third enum), `path_blocked()`. `chase`/`flee_to`/`can_see_feral` are cut (`move_to` tracks moving targets; `exists(enemy)` covers sighting). Every registry entity kind gets a constant; Ferals get nest-bound constants; the table is the registry and 06's tree the acquisition map. Ally-aid programmability (request-reading builtins) is deferred post-v1.
- **Queries follow the seeing/hearing circles** (2026-07-14, Q74 — supersedes Q57's query-radius phrasing; [05-terrain.md](../05-terrain.md)): full returns within **seeing** (the sensor-range stat — total information); **movers only** within **hearing** (× `sense_factor`, ~150% — only moving things make noise, so a heard contact is a position, not a dossier); plus **discovered resource nodes from map knowledge at any range** (a known vein is a fact, not a perception — an undiscovered vein is still a miss, not a secret hit) and **structures/designations from the faction's knowledge pool** (P22, 2026-08-02: own colony state plus granted allies' own, always current; foreign structures are not query-reachable — Q126, none in v1; [05-terrain.md](../05-terrain.md)). LoS blocks both circles. Per-kind bonuses (Combat L3) widen the hearing circle for their kind. **Heard-only contacts are real entity handles** exposing position and distance only — since only bots make noise, they match bot kinds (`enemy`); action verbs accept them (`attack(contact)` closes to engage, resolving on sight), but reading any non-positional property faults (`unknown_contact`) — you heard *something*, you don't know *what*.

- **Messaging is blocking channels, not signals** — a 2×2 API: `send`/`try_send` (one receiver) and `broadcast`/`try_broadcast` (all receivers), each blocking-with-timeout or fire-and-forget (unheard messages are lost); `receive(ch, timeout=n)`/`try_receive` on the other end. No queues. Timeouts fault. **Blocking burns cycles** — waiting is what the CPU is doing, so listening posts and rendezvous have real compute cost. Any value travels; enums + `match` (Tier 6) make channel traffic into typed protocols.
- **`try_receive` is send-only; a broadcast needs a blocking audience** (2026-07-17, answers Q90). A non-blocking `try_receive` consumes only a waiting `send` (one-to-one rendezvous), **never** a waiting `broadcast()` — a broadcast is an all-receivers event, so letting one polling receiver silently swallow it would be semantically wrong; a broadcast is delivered only to receivers actually **blocked** on `receive` at settle time. The selection rule is symmetric on both ends: among competing partners on a channel, the **longest-blocked** wins, ties broken by **lowest entity id** — applied to the receiver a sender pairs with *and* to the sender a `try_receive` pulls from.
- **Enums & `match`** — Rust-style sum types with associated data, Tier 6 construct.
- **Bots have an environment** — a bot-local `key → int` store of engine-defined *policy* parameters (`hurt_line`, `log_min_level`, …) read/written via the `setenv`/`getenv` builtins. Env lives on the bot like XP (survives restarts, redeploys, recalls; dies at destruction); keys are pre-bound constants with defaults and bounds, so reads never fault. The `on boot:` window is the idiomatic config point — the bot's dotfile. **Policy, never stats**: no key makes a bot more capable. Custom hurt thresholds live here (`hurt_line` — re-answers Q41, replacing the short-lived `on hurt(n):` research); the Damaged state penalty stays fixed at 50%, decoupled from the signal.
- **Signal-safety is DELETED; handler windows take any code** (redesign 2026-08-02, supersedes Q49's `signal_safe` flag and Q51's derived-safety/boundedness rules). A window may call any builtin, any `def`, and may loop and recurse; there is no instruction cap, no per-function flag, no deploy-time rejection. Three reasons, in order of weight. **(1) The cap measured the wrong quantity.** Caps bound worst-case *instruction count*, but handler danger is measured in *ticks* — and a blocking verb decouples them completely, so the most dangerous thing anyone writes (a blocking limp to a distant Repair Bay) is one instruction and sails under a cap of six. The design already said as much: handler time was priced by the double-handle rule, never by the cap. **(2) Program memory is the better limiter** — window contents are program source and count against the Q52 hardware bar, so window size already competes with the main program for a buyable budget. That is an economic constraint players can read and invest in, replacing a flat compile-time cliff. **(3) Nothing about determinism rested on it** — every excluded builtin was deterministic and the checks were static, so an unbounded window merely burns the bot's own cycle budget. Handler risk is now priced **entirely by the double-handle rule**: a fault inside a window is an abort, so moving real work into handlers trades "a mistake costs 2 HP" for "a mistake is a wreck" — a genuinely worse deal that teaches itself. `abort` and `recall` stay **fully reserved** (that is template structure, not safety), forced prologues and epilogues are unchanged, and windows remain per-signal unlockable constructs.
- **The analysis survives as information, not prohibition** (same ruling). The editor still shows each `def`'s worst-case instruction count where it is computable, and reports **unbounded** where a loop or recursion makes it undecidable; nothing is greyed out and no deploy fails. The one failure mode with no natural teaching loop — an **unbounded window**, whether an unbounded loop or a **channel** call left to block forever (`timeout=None`; action-blocking verbs like `move_to` always resolve and never qualify), which parks the bot inside the template indefinitely — gets a deploy-time **warning**, never an error. Everything else announces itself with a wreck and a crash dump, which is the intended pedagogy.
- **Logs have levels** — `log(value, level=info)` with five pre-bound level constants (`trace / debug / info / warn / error`). Levels ride the ring buffer, uploads, and Black Boxes; the cloud viewer color-codes and filters by them. Forced crash dumps file at `error`.
- **Banked cycles are capped** — budget clamps to `bank_cap` after every grant. Since Q101 the cap is a **flat constant** guarded by a load-time worst-case check (see Execution Model) rather than a per-bot-per-tile derivation; debt from forced charges is unaffected (charged, not banked). Freeze-forever in front of an unaffordable op stays impossible, so overlays may raise costs freely — a mod that would break it fails at load.
- **Optional parameters are trailing keyword defaults, Python-style** — `log(msg, level=warn)`, `send(ch, val, timeout=100)`; `timeout=None` (the default) means block forever. Applies to builtins and `def`s alike; call sites may pass optionals positionally or by keyword.
- **`None` is an enum, not a null** — the builtin `Option.Some(v)` / `Option.None`, Rust-style, with `None` = `Option.None` (bare `case None:` works in `match`). Optional-typed parameters take the value or `None`; `.expect()` and `match` work on `Option` exactly as on `Result`. The type system stays null-free — absence is always explicit. The former `Recv` enum is retired: `try_receive` returns `Option` — two builtin enums (`Result`, `Option`), no one-off shapes.
- **`None`, `True`, `False` are reserved words** — assigning to them is a parse error; they can never be shadowed. Kind and level constants stay ordinary shadowable names (colliding with `ore` is survivable; a program where `None` is 5 is not worth debugging).
- **Log buffer is hardware** — base 8 entries, +Memory bank.

- **Channel espionage is real, and gated by comm keys**: to touch a foreign channel you need its faction's **comm key** (`analyze()` any faction's wreck — Q76) *plus* the channel name (found in decrypted portions of their code — player and Feral code decrypt by the same salvage-attrition rule, [08-multiplayer.md](../08-multiplayer.md)). With both: `receive` to eavesdrop — or in one-receiver mode outright **steal** the message — and `send` to **spoof**. Defensive protocol craft — rotating names, enum-tag authentication, decoy channels — is the endgame of colony design.
- **Decryption surface = the deployed artifact (answers Q62)** — a color leaks exactly what it deploys: program, handler windows, and transitively-called module functions, tree-shaken at function granularity at deploy time. Unused library code never ships, never leaks, and a function nobody deploys is a secret forever. Shared module text appears under each deploying color's reveal mask; cross-closure composition (the same mechanic as cross-version reading, [08-multiplayer.md](../08-multiplayer.md)) makes shared libraries leak fastest — private forks are opsec.
