[package]
name = "game"
version.workspace = true
edition.workspace = true
description = "Bevy app: rendering, UI, networking (placeholder until sim exists)"

[dependencies]
sim = { path = "../sim" }
bevy = "0.16"
bevy_egui = "0.34"
