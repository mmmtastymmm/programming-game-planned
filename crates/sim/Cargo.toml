[package]
name = "sim"
version.workspace = true
edition.workspace = true
description = "Deterministic world simulation (renderer-free; see docs/07-architecture.md)"

[dependencies]
pyrite = { path = "../pyrite" }
